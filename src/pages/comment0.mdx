The `pay` entrypoint applies a penalty fee of 7% per day beyond deadline.
Archetype language provides types like rational (see `7%`) or duration (see `1d`) types to easily implement readable business rules.