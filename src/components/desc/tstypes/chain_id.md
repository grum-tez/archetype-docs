Class for a chain id value that corresponds to Archetype/Michelson type [`chain_id`](/docs/reference/types#chain_id). Its constructor takes a string value.

For example:
```ts
const cid = new Chain_id("NetXdQprcVkpaWU")
```