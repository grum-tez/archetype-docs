archetype pay_with_penalty(holder : address, cost : tez, deadline : date)

entry pay () {
  const penalty = if now > deadline then 7% * (now - deadline) / 1d else 0;
  transfer ((1 + penalty) * cost) to holder
}