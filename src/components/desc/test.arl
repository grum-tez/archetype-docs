archetype test

asset partitioned identified by k {
  k : nat;
  v : nat
}

asset partitioning identified by a {
  a      : address;
  assets : aggregate<partitioned>
}

entry exec () {
    partitioning.tail(3).removeif(1 >= 2)
}