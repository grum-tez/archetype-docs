# (Pair 0 { })
{
  storage (pair (int %toto) (map %a string int));
  parameter (unit %exec);
  code { UNPAIR;
         DIP { UNPAIR 2 };
         DROP;
         DUP 2;
         PUSH int 3;
         SOME;
         PUSH string "toto";
         UPDATE;
         DIP { DIG 1; DROP };
         DUG 1;
         PAIR 2;
         NIL operation;
         PAIR };
}

